## Getting Started

Install the modules:

```bash
npm install
```

Copy the pdf js express for Web library assets to the public directory (or we can copy it manually too):

```bash
npm run postinstall
```

run the development server:

```bash
npm run dev
# or
yarn dev
```
